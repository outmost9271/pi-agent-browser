# 仅构建期使用代理下载依赖，运行时浏览器访问走标准网络
# compose.yaml 已在 build.args 中配置，如需覆盖在此设置
# HTTP_PROXY=http://host.docker.internal:10808
# HTTPS_PROXY=http://host.docker.internal:10808

# 可选：持久化加密
# AGENT_BROWSER_ENCRYPTION_KEY=your-64-hex-key-here

# 固定版本
AGENT_BROWSER_VERSION=0.36.0
